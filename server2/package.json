{
  "name": "server2",
  "version": "2.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "opn http://localhost:3001 && cross-env NODE_ENV=production node ./server"
  },
  "author": "Vidushan CHOORIYAKUMARAN",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "opn-cli": "^3.1.0",
    "similarity": "^1.1.1",
    "cheerio": "^1.0.0-rc.2",
    "request": "^2.83.0"
  },
  "devDependencies": {
    "cross-env": "^5.1.3"
  }
}
